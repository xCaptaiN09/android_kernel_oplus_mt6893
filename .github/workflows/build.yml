name: Build Kernel

on:
  push:
    branches:
      - sukisu-4.19
      - ksu-next-4.19
      - KSUN-susfs-v2.2.0-manual-hook-mglru
      - KSUN-susfs-v2.2.0-manual-hook
      - KSUN-RUI-susfs-v2.2.0-manual-hook
      - fix/selinux-attr-current-probe-hide
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y bc bison build-essential curl flex gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi git gnupg gperf libncurses5-dev libssl-dev python3 zip zlib1g-dev
        sudo apt-get install -y python2 || true

    - name: Set up Proton Clang
      run: |
        mkdir -p $GITHUB_WORKSPACE/toolchain
        git clone --depth=1 https://github.com/kdrag0n/proton-clang.git $GITHUB_WORKSPACE/toolchain/proton-clang

    - name: Build Kernel
      env:
        TOOLCHAIN: ${{ github.workspace }}/toolchain/proton-clang
      run: |
        bash build_kernel.sh

    - name: Show errors on failure
      if: failure()
      run: |
        test -f build.log
        grep -E "error:|fatal error:" build.log | head -30 || true

    - name: Print build log on failure
      if: failure()
      run: |
        test -f build.log
        tail -100 build.log

    - name: Upload Image.gz-dtb
      uses: actions/upload-artifact@v4
      with:
        name: Image.gz-dtb
        path: out/arch/arm64/boot/Image.gz-dtb

    - name: Upload vmlinux
      uses: actions/upload-artifact@v4
      with:
        name: vmlinux
        path: out/vmlinux

    - name: Create AnyKernel3 Zip
      run: |
        git clone --depth=1 https://github.com/osm0sis/AnyKernel3.git
        cp out/arch/arm64/boot/Image.gz-dtb AnyKernel3/
        cp anykernel_template.sh AnyKernel3/anykernel.sh
        cd AnyKernel3
        rm -rf .git
        zip -r9 ../KSU_Next_OP6893.zip *

    - name: Upload Flashable Zip
      uses: actions/upload-artifact@v4
      with:
        name: KSU_Next_OP6893
        path: KSU_Next_OP6893.zip
